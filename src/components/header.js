import React from "react"
import { Link } from "gatsby"

export default() => (
    <header className="header">
        <h1>
            <Link to="/">Gatsby Starter</Link>
        </h1>
    </header>
)
