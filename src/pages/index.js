import React from "react"
import { Link } from "gatsby"
import Layout from "../components/layout"


export default () => (
    <Layout>
        <p>
            This is a GatsyJS starter project with minimal production-ready configuration.
        </p>
    </Layout>
)
